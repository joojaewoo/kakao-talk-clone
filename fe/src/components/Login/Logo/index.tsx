import React, { FC } from 'react';
import LogoIcon from '../../Reusable/Icons/Logo';
import Container from './styled';

const Logo: FC = () => (
  <Container>
    <LogoIcon />
  </Container>
);

export default Logo;
