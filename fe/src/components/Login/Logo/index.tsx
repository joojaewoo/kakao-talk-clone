import React, { FC } from 'react';
import { LogoIcon } from '../../Reusable/Icons';
import Container from './styled';

const Logo: FC = () => (
  <Container>
    <LogoIcon />
  </Container>
);

export default Logo;
